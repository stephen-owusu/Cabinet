"""Evaluation: runs recorded scenarios against the agent loop and scores
the result.

Depends on cip-core. What it evaluates is wired by cip-bootstrap, never
imported directly, so an eval run exercises exactly what a real process
would run in that run mode.
"""
