[project]
name = "cip-evaluation"
version = "0.1.0"
description = "Runs recorded scenarios against the agent loop and scores the result."
requires-python = ">=3.12"
dependencies = [
    "cip-core",
]

[tool.uv.sources]
cip-core = { workspace = true }

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/cip"]
