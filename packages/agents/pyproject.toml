[project]
name = "cip-agents"
version = "0.1.0"
description = "The orchestration loop that calls a model and its tools."
requires-python = ">=3.12"
dependencies = [
    "cip-core",
]

[tool.uv.sources]
cip-core = { workspace = true }

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/cip"]
